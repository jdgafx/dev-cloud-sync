events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        location / {
            # Default to dashboard proxy later if needed
            return 200 "CloudSync Infrastructure Nginx\n";
        }
    }
}
